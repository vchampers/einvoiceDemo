server:
      port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/einvoice-test?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456a
  redis:
    database: 0
    host: localhost
    port: 6379
    password:

mybatis:
  mapper-locations: classpath:mappers/*.xml

logging:
  level:
      com.example.demo.repository: debug





